// Generated by rust-js from examples/methods.rs. Do not edit.

export const Counter = {
  new(step) {
    return {
      count: 0,
      step
    };
  },
  default_step() {
    return 1;
  },
  tick(counter) {
    counter.count = counter.count + counter.step >>> 0;
  },
  ticked(counter, times) {
    let next = {
      count: counter.count,
      step: counter.step
    };
    for (let i = 0; i < times; i++) {
      Counter.tick(next);
    }
    return next;
  },
  value(counter) {
    return counter.count;
  }
};

export const Light = {
  next(light) {
    if (light === "Red") {
      return "Green";
    } else {
      return "Red";
    }
  },
  is_go(light) {
    return light === "Green";
  }
};

export const Pair = {
  new(a, b) {
    return [a, b];
  },
  sum(pair) {
    return pair[0] + pair[1] | 0;
  }
};

export function counted(step, times) {
  return Counter.value(Counter.ticked(Counter.new(step), times));
}

export function ticking(step) {
  let counter = Counter.new(step);
  Counter.tick(counter);
  Counter.tick(counter);
  return Counter.value(counter);
}

export function lights(n) {
  let light = "Red";
  for (let i = 0; i < n; i++) {
    light = Light.next(light);
  }
  return Light.is_go(light);
}

export function pair_sum(a, b) {
  return Pair.sum(Pair.new(a, b)) + (Counter.default_step() | 0) | 0;
}
//# sourceMappingURL=methods.js.map
