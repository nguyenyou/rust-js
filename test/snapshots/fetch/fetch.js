// Generated by rust-js from examples/fetch.rs. Do not edit.

function $unwrap(value, message = "called `Option::unwrap()` on a `None` value") {
  if (value == null) {
    throw new Error(message);
  }
  return value;
}

const URL = "data:text/plain,Hello from a fetch!";

async function load(url, output) {
  output.textContent = "Loading…";
  const response = await window.fetch(url);
  const text = await response.text();
  const status = String(response.status);
  output.textContent = status + " " + text;
}

export function main() {
  const app = $unwrap(document.getElementById("app"), "the page has an #app");
  const button = document.createElement("button");
  button.textContent = "Fetch";
  const output = document.createElement("output");
  button.addEventListener("click", () => {
    load(URL, output);
  });
  app.append(button);
  app.append(output);
}
//# sourceMappingURL=fetch.js.map
