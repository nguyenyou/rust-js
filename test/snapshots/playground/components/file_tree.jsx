// Generated by rust-js from wasm/web/rust/components/file_tree.rs. Do not edit.

import { FileItem } from "./file_item.jsx";
import { ROW } from "../styles.js";
import { inOrder } from "../tree.js";

export function FileTree({ tree, depth, first, selected, onOpen, onDelete }) {
  const rows = inOrder(tree, first).map((param) => {
    if (param[1].TAG === "Folder") {
      return (
        <li key={`${param[0]}/`} className="flex items-center">
          <div className="w-full">
            <span
              className={`block ${ROW} text-muted`}
              style={{ paddingLeft: (8 + (Math.imul(depth, 12) >>> 0)) >>> 0 }}
            >{`${param[0]}/`}</span>
            <ul>
              <FileTree
                tree={param[1]._0}
                depth={(depth + 1) >>> 0}
                first={first}
                selected={selected}
                onOpen={onOpen}
                onDelete={onDelete}
              />
            </ul>
          </div>
        </li>
      );
    } else {
      return (
        <FileItem
          name={param[0]}
          path={param[1]._0}
          depth={depth}
          open={param[1]._0 === selected}
          root={param[1]._0 === first}
          onOpen={onOpen}
          onDelete={onDelete}
          key={param[1]._0}
        />
      );
    }
  });
  return <>{rows}</>;
}
//# sourceMappingURL=file_tree.jsx.map
